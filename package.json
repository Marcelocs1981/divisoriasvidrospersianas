{
  "name": "imprimiprinter",
  "type": "module",
  "version": "1.0.0",
  "description": "Infraestrutura estática de alto desempenho para imprimiprinter",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "build-search": "rm -rf dist && astro check && astro build && npx pagefind --site \"dist\" && astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.6",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^5.7.3"
  }
}