<section 
  id="escolher" 
  class="section-padrao section-escolher" 
  aria-labelledby="escolher-heading"
>
  <div class="container">
    
<blockquote class="quote-text" cite="https://divisoriasvidrospersianas.com.br">
  <span class="quote-body">
    Escolher especialistas em divisórias de vidro com persianas embutidas para escritório, é a maneira mais 
    <span class="goldgradient">rápida, econômica, segura e tranquila</span> 
    de garantir o sucesso do seu projeto.
  </span> <cite class="quote-author">
    <h3 id="escolher-heading" class="author-title">+25 anos criando ambientes corporativos com vidros</h3>
    <p class="author-subtitle">Especialistas em divisórias de vidro com persianas integradas</p>
  </cite>
</blockquote>

  </div>
</section>

<style>

.section-escolher {
    position: relative;
    overflow: hidden;
  }  

.section-escolher::before {
    content: "";
    position: absolute;
    inset: 0;
    /* Aponta direto e nativamente para a pasta public do seu projeto */
    background-image: url('/assets/bg-escolher-mobile.png'); 
    background-repeat: no-repeat;
    background-position: center center;
    background-size: cover;
    z-index: -1;
    pointer-events: none;
  }

  @media (min-width: 769px) {
    .section-escolher::before {
      background-image: url('/assets/bg-escolher-pc.png');
    }
  }

  /* O restante do CSS dos textos continua exatamente igual */
  .quote-text {
    font-size: clamp(1.5rem, 2.8vw, 2.4rem);
    line-height: 1.2;
    margin: 0;
    font-weight: 400;
    letter-spacing: -0.02em;
    position: relative;
  }

  .quote-body {
    position: relative;
    quotes: none;
  }

  .quote-body::before {
    content: "\201C";
    display: inline-block;
    font-size: clamp(4rem, 6vw, 5.5rem);
    color: #967000;
    line-height: 1;
    margin-bottom: -10px;
    margin-right: 5px;
  }

  .quote-body::after {
    content: "\201D";
    display: inline-block;
    color: #967000;
    font-size: clamp(4rem, 6vw, 5.5rem);
    margin-left: 8px;
    line-height: 1;
    vertical-align: text-top; /* Faz a aspa alinhar perfeitamente no topo da palavra "projeto." */
  }

  .quote-author {
    display: block;
    font-style: normal;
    margin-top: 30px;
    padding-left: 25px;
    border-left: 2px solid #967000; 
  }

  .author-title {
    font-size: clamp(0.95rem, 1.2vw, 1.1rem);
    font-weight: 500;
    color: #ffffff;
    margin-bottom: 4px;
  }

  .author-subtitle {
    font-size: clamp(0.8rem, 1vw, 0.88rem);
    color: #888888;
  }

  @media (max-width: 768px) {
    .quote-author {
      margin-top: 40px;
    }
  }
</style>