---
import { Image } from 'astro:assets';

import imgDesktop from '../assets/divisoria-de-vidro-duplo-com-persiana-embutida-1200px.webp';
import imgMobile from '../assets/divisorias-de-vidros-com-persianas-600px.webp';
---

<section class="anticipation">

  <div class="container">

    <span class="tag">
      IMAGINE POR UM MOMENTO
    </span>

    <h2>
      Seu cliente atravessa a porta, levanta o olhar e percebe que está diante de uma empresa diferente.
    </h2>

    <div class="content">

      <!-- TEXTO -->
      <div class="text">

        <p>
          As divisórias de vidro duplo com persianas embutidas,
          se estendem do piso ao teto — com persianas integradas,
          perfeitamente alinhadas, filtram a luz com elegância cinematográfica.
        </p>

        <p>
          Um toque no smartphone — e a sala de reuniões se torna privada.
          <span class="highlight">
            Outro toque, e a transparência volta.
          </span>
          Silêncio absoluto. Temperatura impecável.
          Nenhum reflexo no monitor.
        </p>

        <p class="strong">
          Antes mesmo da primeira palavra,
          a negociação já começou a seu favor.
        </p>

        <p>
          É exatamente isso que divisórias em vidros com persianas embutidas entregam:
          <b>maior percepção de valor da sua marca.</b>
        </p>

      </div>

      <!-- IMAGEM -->
      <div class="right">

        <picture>

          <source
            media="(max-width: 768px)"
            srcset={imgMobile.src}
          />

          <Image
            src={imgDesktop}
            alt="Divisória de vidro duplo com persiana embutida"
            loading="lazy"
            decoding="async"
            width={1200}
            height={800}
            class="anticipation-img"
          />

        </picture>

      </div>

    </div>

  </div>

</section>

<style>
.anticipation {
  padding: 100px 0;
  background: #050505;
  color: #fff;
  content-visibility: auto;
}

.container {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
}

/* TAG */

.tag {
  display: block;
  margin-bottom: 25px;

  font-size: 11px;
  letter-spacing: 4px;
  color: #ffbf00;

  font-family:
    system-ui,
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    Roboto,
    sans-serif;
}

/* TÍTULO */

h2 {
  max-width: 700px;
  margin-bottom: 55px;
  font-size: 2.5rem;
  line-height: 1.15;
  letter-spacing: 0;
  font-weight: 500;

  color: #eaeaea;

  contain: layout paint;

  font-family:
    Arial,
    Helvetica,
    sans-serif;
}

.gold {
  color: #d4a51a;
}

/* GRID */

.content {
  display: grid;
  grid-template-columns: 1.05fr 1fr;
  gap: 70px;
  align-items: center;
}

/* TEXTO */

.text {
  width: 100%;
}

.text p {
  margin-bottom: 18px;

  font-size: clamp(0.95rem, 1.2vw, 1.08rem);
  line-height: 1.55;
  color: #cfcfcf;

  font-family:
    system-ui,
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    Roboto,
    sans-serif;
}

.highlight {
  color: #fff;
}

.strong {
  color: #fff;
  font-weight: 700;
}

/* IMAGEM */

.right {
  width: 100%;
}

.right picture {
  width: 100%;
  display: block;
}

.anticipation-img {
  width: 100%;
  height: auto;
  display: block;

  border-radius: 10px;

  content-visibility: auto;
  contain-intrinsic-size: 1200px 800px;
}

/* RESPONSIVO */

@media (max-width: 768px) {

  .anticipation {
    padding: 70px 0;
  }

  .container {
    width: 90%;
  }

  .content {
    grid-template-columns: 1fr;
    gap: 35px;
  }

  h2 {
    max-width: 100%;
    margin-bottom: 35px;
    font-size: 1.6rem;
    line-height: 1.15;
  }

  .text p {
    line-height: 1.5;
  }

}
</style>