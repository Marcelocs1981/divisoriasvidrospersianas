<section class="orcamento">

  <div class="container">

    <!-- ESQUERDA -->
    <div class="left">

      <span class="tag">
        PRÓXIMO PASSO
      </span>

      <h2>
        Solicite seu projeto<br />
        com <span>orçamento</span><br />
        <span>sem compromisso.</span>
      </h2>

      <p class="desc">
        Em menos de 24h, um especialista entra em contato para entender seu projeto.
        Visita técnica gratuita, projeto 3D personalizado e a melhor proposta do mercado.
      </p>

      <!-- BENEFÍCIOS -->
      <ul class="benefits">

        <li>
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <path d="M20 6L9 17l-5-5"></path>
          </svg>
          Visita técnica gratuita em São Paulo
        </li>

        <li>
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <path d="M20 6L9 17l-5-5"></path>
          </svg>
          Projeto 3D personalizado sem compromisso
        </li>

        <li>
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <path d="M20 6L9 17l-5-5"></path>
          </svg>
          Cartão BNDES e parcelamento sem juros
        </li>

        <li>
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <path d="M20 6L9 17l-5-5"></path>
          </svg>
          Garantia total de fábrica
        </li>

      </ul>

    </div>

    <!-- DIREITA -->
    <div class="right">

      <form class="form">

        <div class="grid">

          <div class="field">
            <label for="nome">Nome*</label>
            <input type="text" id="nome" name="nome" />
          </div>

          <div class="field">
            <label for="empresa">Empresa</label>
            <input type="text" id="empresa" name="empresa" />
          </div>

          <div class="field">
            <label for="email">E-mail*</label>
            <input type="email" id="email" name="email" />
          </div>

          <div class="field">
            <label for="telefone">Telefone / WhatsApp*</label>
            <input type="tel" id="telefone" name="telefone" />
          </div>

        </div>

        <div class="field">
          <label for="modelo">Modelo de interesse</label>
          <input
            type="text"
            id="modelo"
            name="modelo"
            placeholder="Ex.: Vidro duplo com persiana embutida"
          />
        </div>

        <div class="field">
          <label for="mensagem">Conte sobre seu projeto</label>

          <textarea
            id="mensagem"
            name="mensagem"
            rows="5"
            placeholder="Metragem aproximada, localização, prazo desejado..."
          ></textarea>
        </div>

        <button type="submit">
          SOLICITAR ORÇAMENTO PERSONALIZADO
        </button>

        <p class="privacy">
          Ao enviar, você concorda com nossa política de privacidade.
        </p>

      </form>

    </div>

  </div>

</section>

<style>
.orcamento {
  position: relative;

  padding: 140px 20px;

  background:
    radial-gradient(circle at top left, rgba(150,112,0,0.08), transparent 35%),
    #020202;

  overflow: hidden;
}

.orcamento::before {
  content: "";

  position: absolute;
  inset: 0;

  background:
    radial-gradient(circle at 70% 20%, rgba(150,112,0,0.08), transparent 40%);

  pointer-events: none;
}

/* CONTAINER */
.orcamento .container {
  max-width: 1280px;

  margin: 0 auto;

  display: grid;
  grid-template-columns: 1fr 560px;

  gap: 100px;
  align-items: start;

  position: relative;
  z-index: 2;
}

/* TAG */
.orcamento .tag {
  display: inline-block;

  color: #967000;

  font-size: 11px;
  letter-spacing: 4px;

  margin-bottom: 26px;
}

/* TÍTULO */
.orcamento h2 {
  color: #fff;

  font-size: 72px;
  line-height: 1.08;

  font-weight: 300;

  margin: 0 0 34px;

  font-family: serif;
}

.orcamento h2 span {
  color: #967000;
}

/* TEXTO */
.orcamento .desc {
  max-width: 620px;

  color: rgba(255,255,255,0.72);

  font-size: 1.15rem;
  line-height: 1.9;

  margin-bottom: 50px;
}

/* BENEFÍCIOS */
.benefits {
  list-style: none;

  padding: 0;
  margin: 0 0 60px;
}

.benefits li {
  display: flex;
  align-items: center;
  gap: 14px;

  color: #fff;

  margin-bottom: 20px;

  font-size: 1rem;
}

.benefits svg {
  width: 18px;
  height: 18px;

  stroke: #967000;
  stroke-width: 2;

  fill: none;

  flex-shrink: 0;
}

/* CARD FORM */
.right {
  background: rgba(5,10,15,0.92);
  border: 1px solid rgba(255,255,255,0.06);
  border-radius: 8px;
  padding: 42px;
  backdrop-filter: blur(10px);
  box-shadow:
    0 0 0 1px rgba(255,255,255,0.02),
    0 20px 60px rgba(0,0,0,0.5);
  contain: layout paint; /* NOVO: isola o backdrop-filter da página */
}

/* GRID */
.form .grid {
  display: grid;
  grid-template-columns: 1fr 1fr;

  gap: 20px;
}

/* FIELD */
.field {
  margin-bottom: 22px;
}

.field label {
  display: block;

  color: #fff;

  font-size: 0.92rem;
  font-weight: 500;

  margin-bottom: 10px;
}

/* INPUTS */
.field input,
.field textarea {
  width: 100%;
  background: #050505;
  border: 1px solid rgba(255,255,255,0.08);
  border-radius: 4px;
  color: #fff;
  padding: 14px 16px;
  font-size: 0.95rem;
  outline: none;
  transition:
    border-color 0.25s ease,
    box-shadow 0.25s ease;  /* removido: background 0.25s ease */
  box-sizing: border-box;
}

/* BOTÃO */
.form button {
  width: 100%;
  height: 58px;
  border: 0;
  border-radius: 4px;
  background: linear-gradient(180deg, #c89d37, #967000);
  color: #000;
  font-size: 0.95rem;
  font-weight: 600;
  letter-spacing: 0.5px;
  cursor: pointer;
  transition:
    transform 0.2s ease,
    box-shadow 0.2s ease;  /* removido: opacity 0.2s ease */
  will-change: transform;  /* NOVO */
}

.form button:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 30px rgba(150,112,0,0.2);
}

/* PRIVACIDADE */
.privacy {
  margin-top: 18px;

  text-align: center;

  color: rgba(255,255,255,0.42);

  font-size: 0.8rem;
  line-height: 1.6;
}

/* RESPONSIVO */
@media (max-width: 1100px) {

  .orcamento .container {
    grid-template-columns: 1fr;
    gap: 70px;
  }

  .orcamento h2 {
    font-size: 56px;
  }
}

@media (max-width: 768px) {

  .orcamento {
    padding: 90px 24px;
  }

  .orcamento h2 {
    font-size: 42px;
  }

  .orcamento .desc {
    font-size: 1rem;
  }

  .form .grid {
    grid-template-columns: 1fr;
  }

  .right {
    padding: 28px;
  }
}

@media (max-width: 480px) {

  .orcamento h2 {
    font-size: 34px;
    line-height: 1.15;
  }

  .benefits li {
    font-size: 0.92rem;
  }
}
</style>